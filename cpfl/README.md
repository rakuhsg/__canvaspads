# CanvasPads Foundation Library

## Components

- `RunLoop` component: Provides platform-specific GUI application main loop.
- `RHI` (Rendering Hardware Interface) component: Provides an abstraction layer for any GPUs and graphics APIs.
